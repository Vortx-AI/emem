{
  "name": "emem-mastra-example",
  "version": "0.0.1",
  "private": true,
  "description": "Mastra Agent connected to the emem MCP server (Streamable HTTP). Hunts for algal-bloom hotspots over Lake Erie and reports each cell + fact_cid.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "start": "tsx emem_mcp_geospatial_agent.ts"
  },
  "dependencies": {
    "@mastra/core": "^0.10.0",
    "@mastra/mcp": "^0.10.0",
    "@ai-sdk/openai": "^1.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
